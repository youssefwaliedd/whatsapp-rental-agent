# Milestone 1 — engine, tools, simulator
pydantic>=2.13
python-dateutil>=2.9

# Milestone 2+ (webhook, storage, agent) — install when those land
# fastapi>=0.115
# uvicorn>=0.32
# sqlalchemy>=2.0
# anthropic>=0.40
