# Milestones 1-3 — engine, tools, persistence, simulator, agent
pydantic>=2.13
python-dateutil>=2.9
sqlalchemy>=2.0
Pillow>=10.0          # renders the demonstration vehicle cards

# Model providers. Gemini is the default (free tier); Anthropic is supported
# by the same agent via a second adapter. Install whichever you use.
google-genai>=1.0
anthropic>=0.40

# PostgreSQL. Optional for development (SQLite is the default) and required in
# production: SQLite serialises conversations, measured at 10 simultaneous
# customers taking 10.7s on SQLite against 1.1s on Postgres.
# See tests/concurrency_check.py.
psycopg[binary]>=3.2

# Milestone 4 — WhatsApp Cloud API transport
fastapi>=0.115
uvicorn[standard]>=0.32
httpx>=0.27
cryptography>=44.0     # authenticated encryption for private documents
boto3>=1.35           # optional S3 document backend
